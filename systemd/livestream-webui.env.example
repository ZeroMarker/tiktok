LIVE_WEBUI_HOST=127.0.0.1
LIVE_WEBUI_PORT=8766
RECORDINGS_DIR=/root/tiktok/recordings

# 认证已移除：WebUI 设计为仅在内网/隧道/受控反代后使用。
# 如需恢复认证，在 webui/app.py 的 Handler.authenticated() 中启用校验，
# 并在此重新设置 LIVE_WEBUI_TOKEN。